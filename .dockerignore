# Everything is excluded except what the Dockerfile copies. The data comes from the
# reachfix-data image (Dockerfile.data, which has its own ignore file).
*
!pyproject.toml
!uv.lock
!README.md
!LICENSE
!src/
!schema/
!config/
**/__pycache__
