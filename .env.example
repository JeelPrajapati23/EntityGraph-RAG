# Copy to .env and fill in.
# GROQ_API_KEY: generation (extraction, router classification, synthesis, eval judge) — https://console.groq.com/keys
# HF_TOKEN: embeddings via the Hugging Face Inference API — https://huggingface.co/settings/tokens
GROQ_API_KEY=
HF_TOKEN=
